/**
 * Deprecated compatibility export. Attendance access is now controlled per
 * teacher in the payroll list, so the old global card is intentionally empty.
 */
export function TeacherAttendanceLockCard() {
  return null
}
